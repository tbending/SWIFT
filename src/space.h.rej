diff a/src/space.h b/src/space.h	(rejected hunks)
@@ -414,5 +411,7 @@ void space_free_foreign_parts(struct space *s, const int clear_cell_pointers);
 void space_struct_dump(struct space *s, FILE *stream);
 void space_struct_restore(struct space *s, FILE *stream);
 void space_write_cell_hierarchy(const struct space *s, int j);
+void space_update_unique_id(struct space *s);
+long long space_get_new_unique_id(struct space *s);
 
 #endif /* SWIFT_SPACE_H */
